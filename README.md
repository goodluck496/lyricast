# LyriCast

### Node v20.17.0

- install pnpm
````shell
pnpm install -g pnpm
````
- install electron
````shell
pnpm install -D nx-electron
````

## Build frontend app

`nx run browser:build:production`

## Build electron app

`nx run electron:package`
